# Optional. If unset, /api/calendar returns { configured: false } and the
# Calendar card shows "No calendar configured."
# Google Calendar → Settings → target calendar → "Secret address in iCal format".
# Any public .ics URL works (no OAuth needed for this path).
ICS_FEED_URL=https://calendar.google.com/calendar/ical/.../basic.ics
